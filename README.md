## **Task Overview**

### **Task 1: Division of Two Variables** (Line ~71-76)
Calculate and print the result of dividing variable `a` by variable `b`.
- **Watch out**: Pay attention to data types (int vs float) to get the correct result (0.25)

### **Task 2: Change Array Value Using Pointer** (Line ~96-100)
Change the second value in the `num_array` to 2 using a pointer.
- Use the existing pointer `ptr` or create a new one

### **Task 3: Conditional Check** (Line ~114-119)
Verify if variable `val` is positive AND divisible by 3 using `if-else`.
- Print an appropriate statement to console

### **Task 4: Create Multiply Function** (Line ~18-20)
Write a function at the beginning of the file that:
- Takes two integers as input
- Returns their product as an integer
- Function signature: `int multiply(int a, int b)`

### **Task 5: Complete Sum Function with For Loop** (Line ~36-45)
Complete the `sum()` function by adding a for loop that:
- Iterates through the entire array
- Sums all elements
- Uses array indexing `array[index]`

### **Task 6: Create Custom Header File** (Line ~171-177)
1. Create a new header file `mylocalheader.h`
2. Define a 5x5 2D array with hardcoded values
3. Include this header in the main file
4. Calculate the sum (or average as mentioned in the PDF) of all values in the 2D array